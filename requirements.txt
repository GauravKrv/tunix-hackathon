# Core JAX and TPU dependencies
jax[tpu]>=0.4.20
jaxlib>=0.4.20
flax>=0.7.5
optax>=0.1.7

# Tunix framework (if available via package manager)
# tunix>=0.1.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# Configuration management
pyyaml>=6.0
omegaconf>=2.3.0
hydra-core>=1.3.0

# Model utilities
dm-haiku>=0.0.10
chex>=0.1.8
einops>=0.7.0

# Logging and monitoring
tensorboard>=2.14.0
wandb>=0.15.0
tqdm>=4.66.0

# TPU-specific utilities
cloud-tpu-client>=0.10
cloud-tpu-profiler>=2.0.0

# Data loading and augmentation
tensorflow>=2.14.0
tensorflow-datasets>=4.9.0
torch>=2.1.0
torchvision>=0.15.0

# Distributed training
ray[default]>=2.7.0

# Utilities
matplotlib>=3.7.0
seaborn>=0.12.0
pillow>=10.0.0
h5py>=3.9.0
jsonlines>=4.0.0

# Development and testing
pytest>=7.4.0
pytest-xdist>=3.3.0
black>=23.7.0
isort>=5.12.0
flake8>=6.1.0
mypy>=1.5.0

# Serialization
msgpack>=1.0.5
pickle5>=0.0.12

# Performance profiling
py-spy>=0.3.14
memory-profiler>=0.61.0

# Checkpointing
orbax-checkpoint>=0.4.0

# Type checking
typing-extensions>=4.7.0

# ============================================================================
# Dataset Preparation Pipeline Requirements
# ============================================================================
#
# Note: The dataset preparation pipeline (data/prepare_reasoning_dataset.py)
# uses only Python's standard library and requires no external dependencies.
#
# Minimum Python version for dataset pipeline: 3.7+
#
# Standard library modules used by dataset pipeline:
# - json: JSON parsing and serialization
# - os: File system operations
# - pathlib: Path manipulation
# - argparse: Command-line argument parsing
# - random: Dataset shuffling
# - dataclasses: Data structures
# - typing: Type hints

# ============================================================================
# Gemma2 Training Requirements
# ============================================================================
torch-xla>=2.1.0
transformers>=4.36.0
accelerate>=0.25.0
datasets>=2.14.0
tokenizers>=0.15.0
sentencepiece>=0.1.99
protobuf>=3.20.0
huggingface-hub>=0.19.0
safetensors>=0.4.0

# ============================================================================
# Inference Requirements
# ============================================================================
# torch>=2.0.0 (already listed above with higher version)
# transformers>=4.30.0 (already listed above with higher version)
# accelerate>=0.20.0 (already listed above with higher version)
bitsandbytes>=0.40.0
